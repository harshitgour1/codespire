#include <iostream>
#include <unordered_set>
#include <vector>

using namespace std;

int main() {
    int n;
    cin >> n;

    vector<int> values(n);
    for (int i = 0; i < n; ++i) {
        cin >> values[i];
    }

    int T;
    cin >> T;

    unordered_set<int> seen; // To store the values we've encountered
    for (int i = 0; i < n; ++i) {
        int complement = T - values[i];
        // Check if the complement exists in the set
        if (seen.find(complement) != seen.end()) {
            cout << "MATCH" << endl;
            return 0;
        }
        // Add the current value to the set
        seen.insert(values[i]);
    }

    cout << "NO MATCH" << endl;
    return 0;
}
